services:
  dataoncustomers:
    image: dataoncustomers
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ACCESS_TOKEN: ${ACCESS_TOKEN}
    environment:
      NODE_ENV: production
    ports:
      - 3000:80
